---
title: "Server Actions"
description: "服务端函数"
---

Next.js 中的 **Server Function**（通常被称为 **Server Actions**）是 React Server Components (RSC) 架构下的核心特性。

它允许你在服务器端直接定义并执行异步函数，而无需手动编写传统的 API 路由（如 `/api/xxx`）。



## 定义Server Actions

### 组件内定义

必须在函数体顶部使用 `'use server'` 指令：

```tsx
// app/posts/page.tsx (Server Component)
export default function Page() {
  async function createPost(formData: FormData) {
    'use server'
    const title = formData.get('title')
    // 直接在服务器端操作数据库或调用后端 API
    await db.post.create({ data: { title } })
  }

  return (
    <form action={createPost}>
      <input type="text" name="title" />
      <button type="submit">创建文章</button>
    </form>
  )
}
```

### 独立文件定义

在独立的 `.ts` 或 `.js` 文件顶部添加 `'use server'`：

```js
// app/actions.ts
'use server'

export async function updateUser(userId: string, data: any) {
  // 数据库更新逻辑
  await db.user.update({ where: { id: userId }, data })
}
```



## Usage
