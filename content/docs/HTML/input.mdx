---
title: input
---

## change

`input` 的值由 React 状态控制；修改代码或在预览中输入文字，即可看到变化。

<Sandpack
  template="react"
  files={{
    "/App.js": `import { useState } from "react";

export default function App() {
  const [name, setName] = useState("");

  return (
    <main>
      <label htmlFor="name">姓名</label>
      <input
        id="name"
        value={name}
        onChange={(event) => setName(event.target.value)}
        placeholder="请输入姓名"
      />
      <p>{name ? \`你好，\${name}！\` : "开始输入吧。"}</p>
    </main>
  );
}`,
  }}
/>

## input
