---
title: "缓存命中"
---

## 缓存命中原理

不是缓存“聊天记录”。而是缓存：**输入 Token 前缀**对应的模型计算结果。

- Chat Completions API 虽然无状态，但只要你重复发送相同的 messages 前缀，服务端仍然可以通过 Token 前缀匹配实现缓存命中。
- Responses API解决的是不需要客户端重新传历史消息。而不是缓存命中。其缓存命中原理也是根据前缀匹配缓存命中。

