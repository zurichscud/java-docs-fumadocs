---
title: "BufferedWriter"
---

`BufferedWriter` 是**字符缓冲输出流**，继承自 [`Writer`](/docs/java/api/writer)。它是一个**包装流**：包装一个底层 `Writer`（通常是 [`FileWriter`](/docs/java/api/file-writer)），数据先写入内存缓冲区，缓冲区满时才批量写入文件，减少频繁的磁盘 I/O，并额外提供了按平台换行的 `newLine()`。

## Usage

其余写入方法（`write()`、`flush()`、`close()`）继承自 `Writer`；其中 `flush()` 会把缓冲区中的数据交给底层流，`close()` 会**一并关闭**被包装的底层流。

```java
try (BufferedWriter writer = new BufferedWriter(new FileWriter("test.txt"))) {

    writer.write("hello");
    writer.newLine();
    writer.write("你好");

}
```

## Constructor

### BufferedWriter(Writer out)

```java
BufferedWriter writer = new BufferedWriter(new FileWriter("test.txt"));
```

- out：被包装的底层字符输出流，通常是 `FileWriter`
- 使用默认缓冲区大小（8192 个字符）

### BufferedWriter(Writer out, int sz)

```java
BufferedWriter writer = new BufferedWriter(new FileWriter("test.txt"), 1024);
```

- sz：自定义缓冲区大小（字符数），必须大于 `0`，否则抛出 `IllegalArgumentException`

## Methods

### newLine()

写入一个**平台相关的换行符**（Windows 上是 `\r\n`，Linux/macOS 上是 `\n`）。

```java
public void newLine() throws IOException
```

- 需要换行时推荐使用它，而不是手动写 `"\n"`，这样可以保证跨平台
- 典型用法见上方 [Usage](#usage) 中的换行写入示例
