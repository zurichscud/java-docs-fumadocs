---
title: "BufferedReader"
---

`BufferedReader` 是**字符缓冲输入流**，继承自 [`Reader`](/docs/java/api/reader)。它是一个**包装流**：包装一个底层 `Reader`（通常是 [`FileReader`](/docs/java/api/file-reader)），在内存中开辟缓冲区，减少频繁的磁盘 I/O，并额外提供了按行读取的 `readLine()`。

## Usage

```java
try (BufferedReader reader = new BufferedReader(new FileReader("test.txt"))) {

    String line;

    while ((line = reader.readLine()) != null) {
        System.out.println(line);
    }

}
```

## Constructor

### BufferedReader(Reader in)

```java
BufferedReader reader = new BufferedReader(new FileReader("test.txt"));
```

- in：被包装的底层字符输入流，通常是 `FileReader`
- 使用默认缓冲区大小（8192 个字符）

### BufferedReader(Reader in, int sz)

```java
BufferedReader reader = new BufferedReader(new FileReader("test.txt"), 1024);
```

- sz：自定义缓冲区大小（字符数），必须大于 `0`，否则抛出 `IllegalArgumentException`



## Methods

### readLine()

读取一行文本，返回值**不包含**行尾的换行符；到达文件末尾时返回 `null`。

```java
public String readLine() throws IOException
```

- 一行以 `\n`、`\r` 或 `\r\n` 结尾，三种行结束符都能正确处理
- 典型用法见上方 [Usage](#usage) 中的按行读取循环
